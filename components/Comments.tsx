// Comments stub - returns null since comments are disabled
// Add actual implementation when needed (e.g., Giscus, Utterances)
export default function Comments({ slug }: { slug: string }) {
  return null
}
